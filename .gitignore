# Local path overrides (sibling layout) — see projects-map.local.yaml.example
projects-map.local.yaml

# Kilo Code local config (tokens / paths)
kilo.jsonc

# Local IDE / agent session artifacts
.cursor/
.superpowers/
